import { Link, Outlet} from "react-router-dom";
import { AuthStatus } from "../Auth";


const Layout = () =>{
    return (
        <> 
            <Outlet/>
            
        </>
    )
}

export default Layout;