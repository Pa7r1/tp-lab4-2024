import {createTheme} from "@mui/material"

const Theme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#b6293a",
        },
        secondary :{
            main : "#565656"
        },
    }
})

export default Theme